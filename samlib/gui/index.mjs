import { NumberInput } from "./NumberInput.mjs";
import { SamSelector } from "./Selector.mjs";

/**
 * Un number input plus joli et pratique
 * <sam-numberinput value="valeur" min="minimum" max="maximum"/>
 * @event change quand la valeur change
 */

/**
 * Un selecteur comme <select> mais pas avec un menu déroulant.
 * Tout est visible directement.
 * <sam-selector>
 *  <sam-option>...</sam-option>
 *  <sam-option selected=true>...</sam-option>
 *  <sam-option>...</sam-option>
 * </sam-selector>
 * @event select quand une option est sélectionnée
 */